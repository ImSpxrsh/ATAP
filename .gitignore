# Data — large public downloads, never committed (see docs/DATA.md to fetch)
data/raw/
data/processed/
!data/raw/.gitkeep

# Python
__pycache__/
*.py[cod]
.venv/
venv/
env/
*.egg-info/
.ipynb_checkpoints/

# OS
.DS_Store

# Results are regenerated by the scripts; keep the dirs, ignore heavy artifacts
results/**/*.csv
# figures are small and useful as a visual record — keep them tracked
